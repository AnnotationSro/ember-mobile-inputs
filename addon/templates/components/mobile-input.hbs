{{#component inputComponentType
  value=value
  format=format
  disabled=disabled
  readonly=readonly
  showOn=showOn
  decimalMark=decimalMark
  min=min max=max
  formattedPlaceholder=placeholder
  onValueChanged=onValueChanged
  selectOnClick=selectOnClick
  onBlurChanged=onBlurChanged
  onBlur=onBlur
  options=options
  flatpickrOptions=flatpickrOptions
  autocomplete=autocomplete
  allowNegative=allowNegative
  pattern=pattern
  imaskOptions=imaskOptions
  dataInput=dataInput
  neverNative=neverNative
  mobileType=mobileType
  onMobileInputVisibleChanged=onMobileInputVisibleChanged
}}
  {{yield}}
{{/component}}
